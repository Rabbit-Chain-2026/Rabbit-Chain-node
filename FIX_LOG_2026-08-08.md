# RabbitChain 节点问题修复日志

日期：2026-08-08
范围：BlockTime 迁移（结算进共识）过程中发现并修复的节点问题

## 1. [严重] 节点启动自死锁 → RPC 全挂

- **现象**：节点主流程卡在 `NetworkService::new`，`p2p-peer-id` 永不创建；全部 tokio worker 线程 futex 阻塞，RPC 间歇性全部超时（含 `rabbit_clientVersion`），外部矿工 `rabbit_getWork` 全部失败。
- **根因**：`rabbitnet::configure_global_block_persistence` 中
  `for block in GLOBAL_BLOCKS.read().values().cloned().collect::<Vec<_>>()`
  —— Rust 临时值生命周期：`read()` 返回的 guard 存活**整个 for 语句（含循环体）**，而循环体内 `store_block_body → update_global_block_body_commitments` 需要 `GLOBAL_BLOCKS.write()` → **写锁等待自己持有的读锁 = 自死锁**（parking_lot RwLock 不可重入）。
- **定位**：5 轮无缓冲 `eprintln!` 插桩逐层锁定（persistence → peer-id → store_block_body → GLOBAL_BLOCKS 写锁）。
- **修复**：先快照 `Vec` 再循环：
  ```rust
  let blocks_snapshot: Vec<Block> = { let g = GLOBAL_BLOCKS.read(); g.values().cloned().collect() };
  for block in blocks_snapshot { ... }
  ```
- **验证**：peer-id 正常创建、节点完整启动、RPC 稳定；205 测试全过。

## 2. [严重] PoW 挖矿协议缺陷：矿工永远无法提交有效 share

- **现象**：外部矿工（`rabbitchain mine`）反复 `Share rejected: invalid_pow_hash`，区块高度永远为 0。
- **根因**：`rabbit_getWork` 返回**父块难度**且**无时间戳**；`rabbit_submitWork` 的 canonical 校验却用**调整后难度 + 当前时间 + 矿工标签**构造头 → 矿工哈希与校验头字段不一致（difficulty/timestamp/extra_data 三处）。
- **修复**（节点 + 矿工协调）：
  - `MiningWork` 新增 `header_timestamp` + 调整后 `block_difficulty`；
  - `rabbit_getWork` 返回 `timestamp` / `gas_limit` / 新块难度；
  - `rabbit_submitWork` 用模板值（不再重算 adjust）；
  - 矿工 `compute_pow_hash_from_work` 用 `extra_data = miner_label` 与节点一致。
- **验证**：外部矿工秒出块（18 秒产出 4000+ 区块）。

## 3. [中] 矿工 hex 解析

- `parse_hex_u256` 拒绝奇数位 hex（如 genesis `difficulty: "0x0"` 报 `Odd number of digits`）→ 解析前补前导零。

## 4. [中] 交易结果 block_hash 恒为零

- **根因**：`rabbit_submitWork` 中结果写入发生在 `header.hash = header.compute_hash()` 之前。
- **修复**：先算 header.hash 再写结果（`rabbit_getComputeTxResult` 返回真实 block_hash）。

## 5. [中] BlockTime 迁移后交易结果不持久化

- **根因**：提交改纯入队后，结果只写 RpcApi 内存 map，重启即失。
- **修复**：区块执行时异步补写 redb `put_tx_result` → 重启后 `rabbit_getComputeTxResult` 可查。

## 6. [中] Mint 与 fee 要求自相矛盾（Mint 永不可提交）

- **根因**：生产 profile（devnet/testnet/mainnet）强制 `require_fee_for_compute_tx`（fee 字段必须非零），但执行器策略明令 Mint 禁止携带任何费用（`fee/max_fee/priority_fee` 全 0）→ Mint 同时满足两者不可能。
- **修复**：`rabbit_submit_compute_tx` 对 `Command::Mint` 豁免 fee-required 与 fee 校验（Mint 是价值创造命令）；修正对应测试（Mint fee-less 接受 / Transfer fee-less 拒绝）。

## 7. [中] blockchain 模块死代码复活

- `blockchain/`（chain/sync/fork_choice）与 `state::executor` 从未在 `lib.rs` 声明，**从未被编译**，链校验路径形同虚设。
- 复活后修复：fork_choice 生命周期错误；2 个坏测试（断言错误 + 无有效 PoW → 用真实挖矿 nonce 重写）。

## 8. BlockTime 结算进共识（状态执行器）

- `StateExecutor::execute_block` 真执行区块体 ComputeTx → 生成 receipts → 与区块体声明**逐笔校验**（任何节点重放一致，篡改即拒）。
- `execute_txs`（产块方用，返回 receipts + gas）+ `new_basic_executor`（共享存储/域）。
- `Blockchain::insert_block → validate_block` 对含计算交易的区块执行 + state_root 校验（空区块兼容）。
- 费用（gas × base_fee，封顶 max_fee）在区块执行时计入国库账户（`governance::treasury_address`）。

## 9. ComputeTx 线格式（`spec_json`）

- rabbitcore serde 派生输出原始字节数组；节点手写解析器只接受 COMPUTE_JSON_SPEC 线格式（hex 字符串 / tagged 枚举）。**任何提交必须经 `compute_tx_to_spec_json`**（`rabbitcore::compute::spec_json`）。

## 10. 游戏域注册补全

- `GAME_DOMAIN`（0x6A7A7A00）在 `RpcApi::new` 注册后，补全真实启动路径 `build_default_rpc_api`（否则生产节点提交游戏域交易报 `Domain is not registered`）。

## 11. [严重] 外部矿工（mining-stack）与节点 PoW 契约不一致 → 永远无法出块

- **现象**：`cli_mining_smoke.sh` / `mining_e2e.sh` 实测：pool 层全部 `low_difficulty_share` 拒绝（1 秒 1 次，区块高度恒为 0），节点收不到任何 share。
- **根因**（三处契约不一致，均为 sha256d canonical PoW 迁移引入）：
  1. **池最低难度未纳入矿工搜索目标**：节点 getWork 在全 FF 目标（difficulty 下限）下 `target=all-FF`，矿工 effective target = `stricter(job, local)` 仍为全 FF → **每次哈希都提交**，而 pool 的 `POOL_MIN_SHARE_TARGET`（要求 `hash[0]==0x00`，1/256）把绝大多数拒掉 → 有效算力约 1 H/s。
  2. **extra_data 未含 miner label**：节点 `rabbit_submitWork` canonical 校验用 `extra_data = miner`；mining-stack 的 `compute_pow_hash_v1` 预映像写 `extra_data len=0` 且无内容 → 即使过 pool 门槛也会被节点 `invalid_pow_hash` 拒。
  3. **getWork 字段名不匹配**：节点返回 `base_fee_hopps`，矿工（CLI 与 mining-stack）读 `base_fee_per_gas` → 基础费偏离 1 gwei（INITIAL_BASE_FEE）后全部 share 哈希不匹配。
- **修复**：
  - mining-stack：effective target = `stricter(stricter(job, local), POOL_MIN_SHARE_TARGET)`；`compute_pow_hash_v1` 增加 `miner_id` 参数并写入预映像（extra_data len + bytes）。
  - 节点 `rabbit_getWork`：新增 `base_fee_per_gas`（`parent_base_fee.max(INITIAL_BASE_FEE)` 全精度）；`difficulty` 由 `as_u128()`（精度截断）改为 `to_big_endian()` 全 32 字节。
- **验证**：mining-stack 60 测试（含 golden 向量）✅；`cli_mining_smoke.sh` 全绿（出块 + 区块时间执行 + 输出/对象可查）✅。

## 12. [中] 本地矿工（`run --mine`）单节点 120 秒白等

- **现象**：无对等节点时 `--mine` 的矿工线程迟迟不启动（sandbox 里观察不到出块）。
- **根因**：`wait_for_mining_alignment` 只有"有 peers 且追上"才提前返回；无 peers 时死等 `LOCAL_MINER_STARTUP_WAIT_SECS = 120` 秒超时，且该等待阻塞在矿工线程 spawn 之前。
- **修复**：`peer_count == 0` 立即返回（solo 模式直接开矿）；矿工循环内部已有的"peers 高 8 块暂停"防分叉保护保留。另注意 `--mine` 必须配 `--rpc-auth-token`（无 token 时 `rabbit_submitWork` 按设计返回 -32001）。
- **验证**：单节点 `--mine --rpc-auth-token ...` 秒出块（约 760 块/秒）。

## 13. 测试脚本适配 BlockTime 语义

- `cli_mining_smoke.sh` / `mining_e2e.sh` 原在提交后立即断言 `compute get ok=true` 与 `rabbit_getOutput/getObject` —— SubmitTime 语义；BlockTime 下交易只入队，需在矿工打包后才可查。
- **修复**：提交后断言 `"queued": true`；`compute get` / receipt / output / object 断言全部移到"矿工出块验证"之后。

## 验证

- 红线守护 `scripts/no_silent_fallback.sh` ✅；rabbitcore/rabbitnet/rabbitapi **205 测试** ✅；rabbitcli check ✅；mining-stack **60 测试** ✅
- 链上验证脚本 `shanhai-onchain-mmo/scripts/verify_onchain.sh`：结算交易真实进块（真实 block_hash + Success + output_refs）、对象上链存活、国库收税（balance = max_fee 封顶）、**重启后结果与对象均持久化** ✅
- 外部矿工链路 `scripts/cli_mining_smoke.sh` ✅（出块 + 区块时间执行 + 输出/对象可查）；本地矿工 `run --mine` 单节点秒出块 ✅
